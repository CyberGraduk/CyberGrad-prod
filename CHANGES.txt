CyberGrad — V3.8 Changes
========================
Date: June 2026

FIX 1 — CV Checker SyntaxError (critical)
  Root cause: Old autoUnlockForLoggedInUser function body was left dangling
  outside any function after refactor. await at top level of <script> tag
  is a SyntaxError — crashed entire JS file.
  Result: selectRole was never defined, role buttons did nothing.
  Fix: Removed dangling code block entirely.

FIX 2 — Dashboard greeting fallback
  "Hello, there." → "Hello, Grad."
  Matches brand identity, feels intentional not broken.

FIX 3 — Dashboard cleanup
  Removed "Join the Industry-Ready programme" card
  Removed "Community / WhatsApp Group" card

FIX 4 — Sectors & Roles back button
  "Back to Dashboard" now hidden by default
  Only shows when user is logged in (showContent() reveals it)
  Unauthenticated users see gate screen with no back button

VERSION TAG — v3.8 is live

FILES CHANGED
- cv-checker.html (SyntaxError fix)
- dashboard.html (greeting, removed cards)
- sectors.html (back button auth-gated)
- roles.html (back button auth-gated)
- index.html (version tag)
