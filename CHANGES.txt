CyberGrad — V3.4 Changes
========================
Date: June 2026

1. AUTH FLOW — Enterprise grade login validation
   login.html: Before sending magic link, checks students table
   If email not found → "No account found. Create one first." error shown
   Signup link shown below error
   No magic link sent to unregistered emails
   
2. AUTH CALLBACK — Profile completeness check
   After magic link verified, checks students table for first_name
   If complete profile → dashboard.html
   If incomplete/missing → signup.html?complete=1
   
3. SIGNUP — Complete profile mode (?complete=1)
   Detected via URL param after magic link for incomplete profiles
   Email field hidden (pre-filled from session)
   Heading changes to "Complete your profile"
   On submit → saves name to students table + user_metadata
   
4. LINKEDIN FOOTER — Fixed across all pages
   All pages now point to: https://www.linkedin.com/company/cybergrad/
   Fixed generic linkedin.com links
   Contact section in footer also fixed

VERSION TAG — v3.4 is live

FILES CHANGED
- login.html (email pre-check, error message, signup link)
- auth-callback.html (profile check, handleSession function)
- signup.html (complete=1 mode detection)
- All pages (LinkedIn URL fixed)
- index.html (version tag)
