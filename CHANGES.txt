CyberGrad — V3.5 Changes
========================
Date: June 2026

1. DASHBOARD — Name fix
   Always queries students table first for first_name
   Falls back to user_metadata if students table blocked
   If still no name — shows inline banner with first + last name inputs
   Banner inserts after topbar, visible immediately
   Save button updates students table + user_metadata
   Banner disappears after save

2. DASHBOARD — Logout fixed
   Was redirecting to index.html
   Now redirects to login.html

3. LOGIN — Cross-tab magic link detection
   onAuthStateChange listener added
   If user clicks magic link in another tab, login page auto-redirects to dashboard
   No more stuck "Check your inbox" screen

4. SUPABASE ACTION REQUIRED
   Run this to allow login page to check if email exists:
   create policy "Anyone can check if email exists"
     on students for select
     using (true);

VERSION TAG — v3.5 is live

FILES CHANGED
- dashboard.html (name fix, logout fix, cross-tab detection)
- login.html (cross-tab detection)
- index.html (version tag)
