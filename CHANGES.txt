CyberGrad — V3.9 Changes
========================
Date: June 2026

FIX 1 — Dashboard greeting (root cause found)
  TWO loadUser() functions were defined — second overrode first
  Second had firstName || 'there' fallback
  Fixed: single loadUser() with 4-source name resolution:
  1. getUser() — freshest user_metadata
  2. localStorage cache
  3. Session metadata
  4. students table query
  After finding from students table, saves to user_metadata for future
  Fallback: "Hello, Grad." never "Hello, there."

FIX 2 — Pricing
  Starter: Added WhatsApp Community Access
  Industry-Ready: "Everything in Starter" as first feature
  Short list and full list both updated

FIX 3 — Topmate link updated everywhere
  https://topmate.io/rakeshelamaran/2176589?coupon_code=UKCyb3r
  Updated in index.html FAQ and pricing.html FAQ

FIX 4 — Signup saves name to user_metadata
  signInWithOtp now passes data: { first_name, last_name, university, status }
  Also calls updateUser() after OTP for existing users
  Ensures getUser() returns name on next dashboard load

VERSION TAG — v3.9 is live

FILES CHANGED
- dashboard.html (duplicate loadUser removed, getUser fix)
- index.html (pricing, topmate link, version tag)
- pricing.html (topmate link)
- signup.html (user_metadata on signup)
- api/send-report.js (green → passCol fix)
